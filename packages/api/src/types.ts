export type Env = {
  CLERK_SECRET_KEY: string;
  CLERK_PUBLISHABLE_KEY: string;
  CONVEX_URL: string;
  CONVEX_DEPLOY_KEY: string;
};

export type Variables = {
  clerkId: string;
  clerkToken: string;
};
